```jsx
import { Flex, Space } from '@kogaio/Responsive'

const BatchButtonExamples = () => {
  return (
    <Flex alignItems='center' justifyContent='center'>
      <BatchButton
        effect='opacity'
        icon='check_circle'
        minWidth='112px'
        onClick={() => alert('Clicked!')}
        title='Select All'
      />
    </Flex>
  )
}
;<BatchButtonExamples />
```
