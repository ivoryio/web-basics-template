```jsx
import { useState } from 'react'
import { Card, Space, Typography } from '@kogaio'
import { useBoolean } from '@shared-utils/hooks/useBoolean'

const OverlayExample = () => {
  const [isVisible, setVisible] = useBoolean(false)
  return (
    <Space mx='auto' p={4}>
      <Card
        alignItems='center'
        display='flex'
        flexDirection='column'
        height='300px'
        justifyContent='center'
        position='relative'
        textAlign='center'
        variant='white'
        width={1 / 2}>
        <Typography as='h3' variant='h3'>
          This is a card with an overlay on top of it
        </Typography>
        <Typography as='h6' variant='h6'>
          Its purpose is to show a loading state and to prevent interactions
          with the card while your action is pending
        </Typography>
        <Overlay id='overlay-example' visible />
      </Card>
    </Space>
  )
}
;<OverlayExample />
```
