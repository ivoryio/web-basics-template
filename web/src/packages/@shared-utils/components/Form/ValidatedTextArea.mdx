```jsx
import { Box, Flex, Space, Typography } from '@kogaio'
import { ActionButtons, Form } from '@shared-utils/components'

const ValidatedTextAreaExample = () => {
  return (
    <Flex justifyContent='center' width={1 / 2}>
      <Form
        formStyle={{ width: '100%' }}
        initialValues={{ description: '' }}
        onSubmit={(values, helpers) => {
          helpers.setSubmitting(false)
          alert(JSON.stringify(values, null, 4))
        }}>
        {({ isSubmitting, resetForm, values: { description } }) => (
          <Box>
            <Form.TextArea
              autoComplete='nope'
              id='validated-textarea-example'
              label='Description'
              name='description'
              placeholder='Your description here'
              required
              rows={4}
              width={1}
            />
            <Space mt={4}>
              <ActionButtons
                saveLabel='Create'
                cancel={resetForm}
                loading={isSubmitting}
              />
            </Space>
          </Box>
        )}
      </Form>
    </Flex>
  )
}
;<ValidatedTextAreaExample />
```
