```jsx
import { useState } from 'react'
import { Flex, Space } from '@kogaio/Responsive'

const TagInputExamples = () => {
  const [value, setFieldValue] = useState([])
  return (
    <Space mx='auto'>
      <Flex alignItems='center' justifyContent='center' width={1 / 2}>
        <TagInput
          autoComplete='nope'
          containerStyle={{ width: 1 }}
          id='tag-input-example'
          onChange={newValue => setFieldValue(newValue)}
          placeholder='Tags example'
          value={value}
        />
      </Flex>
    </Space>
  )
}
;<TagInputExamples />
```
