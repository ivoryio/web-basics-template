```jsx
import { useState } from 'react'
import { Flex, Space } from '@kogaio'

const RadioButtonExamples = () => {
  const [selectedCheckbox, selectCheckbox] = useState('')
  return (
    <Space mx='auto'>
      <Flex alignItems='center' width={1}>
        <RadioButton
          checked={selectedCheckbox === 'check1'}
          id='radio-button-example1'
          label='Button1'
          onChange={ev => selectCheckbox(ev.target.value)}
          value='check1'
        />
        <Space ml={3}>
          <RadioButton
            checked={selectedCheckbox === 'check2'}
            id='radio-button-example2'
            label='Button2'
            onChange={ev => selectCheckbox(ev.target.value)}
            value='check2'
          />
          <RadioButton
            checked={selectedCheckbox === 'check3'}
            id='radio-button-example3'
            label='Button3'
            onChange={ev => selectCheckbox(ev.target.value)}
            value='check3'
          />
        </Space>
      </Flex>
    </Space>
  )
}
;<RadioButtonExamples />
```
